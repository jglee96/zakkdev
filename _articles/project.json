{
  "name": "articles",
  "sourceRoot": "_articles",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rsync -av _articles dist/ --exclude project.json"
      }
    }
  }
}
